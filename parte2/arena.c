#include "maq.h"
#define EXERCITO 500

//arena.h -> criar uma array de ponteiros para os exercitos

Maquina *InsereExercito (int nexercitos) {
	Maquina *tropas[nexercitos];//isso ta errado
	for (int i=0; i<nexercitos; i++) {
		for (int j=0; j<EXERCITO, j++) {
			tropas = cria_maquina(<argumento>);
			tropas->time = i;
			tropas->posicao = j;
		}
	}
	return tropas;
}

void RemoveExercito (int time, int nexercitos) {
	
	destroi_maquina()
}